# Deterministic daemon settings for every machine that builds Cat65.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Reuse task outputs across builds and run independent tasks in parallel.
org.gradle.caching=true
org.gradle.parallel=true
